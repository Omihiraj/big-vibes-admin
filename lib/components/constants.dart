import 'package:flutter/material.dart';

const primaryColor = Color(0xFF00DDF7);
const secondaryColor = Color(0xFFFEF473);
const Color bgColor = Color.fromRGBO(243, 243, 243, 1);
const double headlineText = 32;
